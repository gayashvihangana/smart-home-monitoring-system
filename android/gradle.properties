org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin incremental compilation is disabled because its cache files fail to
# close on Windows, breaking :firebase_database:compileDebugKotlin with
# "Could not close incremental caches ... class-fq-name-to-source.tab".
# Costs a little rebuild time; buys a build that actually completes.
kotlin.incremental=false
